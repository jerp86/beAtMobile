import React from 'react';

import { ButtonText, Container } from './styles';

export const Button = () => {
  return (
    <Container>
      <ButtonText>Confirmar</ButtonText>
    </Container>
  );
};
